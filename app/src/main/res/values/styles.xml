<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="supply_anim_style">
        <item name="android:windowEnterAnimation">@anim/supplyshow</item>
        <item name="android:windowExitAnimation">@anim/supplyhide</item>
    </style>

    <declare-styleable name="UnderlinedTextView" >
        <attr name="underlineWidth" format="dimension" />
        <attr name="underlineColor" format="color" />
    </declare-styleable>

    <declare-styleable name="ColorSelectBox" >
        <attr name="Color" format="color"/>
    </declare-styleable>

    <declare-styleable name="NoteView" >
        <attr name="isEdit" format="boolean"/>
    </declare-styleable>

</resources>
